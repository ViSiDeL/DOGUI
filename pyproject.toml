[project]
name = "DOGUI"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["src/api", "src/model-gen"]

[tool.uv.sources]
dogui-modelgen = {workspace = true}
dogui-api = {path = "src/api"}